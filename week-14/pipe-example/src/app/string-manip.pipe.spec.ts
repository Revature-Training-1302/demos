import { StringManipPipe } from './string-manip.pipe';

describe('StringManipPipe', () => {
  it('create an instance', () => {
    const pipe = new StringManipPipe();
    expect(pipe).toBeTruthy();
  });
});
