
// use commas to separate parameters in a function
function hello(x, y, z) {
    console.log(x);
    console.log(z);
    console.log(y);
}

// use commas to separate fields in an object
let person = {
    name: 'bob',
    age: 30,
    job: 'programmer'
}

let x = [1,2,3,4];