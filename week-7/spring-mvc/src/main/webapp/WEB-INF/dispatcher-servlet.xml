<?xml version="1.0" encoding="UTF-8" ?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/mvc
       http://www.springframework.org/schema/mvc/spring-mvc-4.3.xsd
       http://www.springframework.org/schema/context
       http://www.springframework.org/schema/context/spring-context.xsd">

    <!-- Telling Spring MVC where to scan for controllers: -->
    <context:component-scan base-package="com.revature.controller"/>

    <!-- View Resolve, where do we look for views (JSP files)? -->
    <bean class = "org.springframework.web.servlet.view.InternalResourceViewResolver" name = "viewResolve">
        <!-- Every view that we have will be in this location: -->
        <property name="prefix" value = "WEB-INF/views/"/>
        <!-- Every view will end with the .jsp extension -->
        <property name="suffix" value = ".jsp"/>
        <!-- "hello" -> "WEB-INF/views/hello.jsp" -->
    </bean>


</beans>