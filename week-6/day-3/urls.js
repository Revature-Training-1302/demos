// we define values that we might need in other files:
export var star_wars_url = 'https://swapi.dev/api/';

// define different urls to use elsewhere in the project
export var temp_url = 'localhost://';
export var prod_url = 'aws.dgegew://';