let fname = 'rory';
let lname = 'eiffe';

let fullname = fname + ' ' + lname;

// console.log(fullname);

// with template literals, we can simplify the syntax here:
console.log(`${fname} ${lname}`)

let id = 13;
console.log(`localhost:8080/pets/${id}`);